#include<stdio.h>
#define a (l*b)
int main()
{
    int l,b;
    printf("enter the length and breadth:");
    scanf("%d%d",&l,&b);
    int area=a;
    printf("area of rectangle=%d",area);
    return 0;
}